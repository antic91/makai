import { PositionOneBlogDirective } from './position-one-blog.directive';

describe('PositionOneBlogDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionOneBlogDirective();
    expect(directive).toBeTruthy();
  });
});
