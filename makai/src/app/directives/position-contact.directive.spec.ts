import { PositionContactDirective } from './position-contact.directive';

describe('PositionContactDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionContactDirective();
    expect(directive).toBeTruthy();
  });
});
