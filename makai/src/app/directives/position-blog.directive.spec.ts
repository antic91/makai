import { PositionBlogDirective } from './position-blog.directive';

describe('PositionBlogDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionBlogDirective();
    expect(directive).toBeTruthy();
  });
});
